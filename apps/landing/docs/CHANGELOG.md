# Changelog — Marketing site (`apps/landing`)

Notable changes to the **public Ed & Sy marketing website** (homepage, services, case studies, blog, SEO pages).

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/). Version in `package.json` is the reference when you cut releases.

---

## [Unreleased]

### Added

- **`docs/`** changelog (this file) aligned with monorepo doc layout.
- **Tasks:** `tasks/tasks.json` and `tasks/README.md` updated March 2026 to reflect shipped pages.

### Changed

- **README** rewritten for the current SvelteKit marketing app (replaces outdated “demo platform” copy).
- **PRD** document status line: clarifies marketing PRD vs Ed & Sy Admin product in `apps/admin`.

### Notes

- Deeper content prompts and layout references remain under `docs/` (e.g. `homepage.md`, `prompts/`). For cross-app release notes, see [`docs/CHANGELOG.md`](../../../docs/CHANGELOG.md) at the repo root.
