# Marketing site — Taskmaster tasks

Tasks for the Ed & Sy public site (`apps/landing`) live in `tasks.json` under the **`digital-agency`** tag. They are aligned with [PRD.md](../PRD.md).

**CLI:** `task-master list -f apps/landing/tasks/tasks.json`

**MCP:** Point `file` to `apps/landing/tasks/tasks.json` and `projectRoot` to the repo root.

As of March 2026, the five roadmap items in this file are marked **done** to reflect shipped routes and pages. Open product work for demos and billing is tracked in **`apps/admin/tasks/tasks.json`**.
